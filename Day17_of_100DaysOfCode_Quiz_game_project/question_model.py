class Question:
    def __init__(self, q_text, q_answer):
        self.text = q_text       # attribute 1
        self.answer = q_answer    # attribute 2
# these 2 attributes will get initialized when we create a new object from "Question" class.
